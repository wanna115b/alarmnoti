<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wanna.app.alarmnoti">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.wanna.app.alarmnoti.activity.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.wanna.app.alarmnoti.activity.AlarmListActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name="com.wanna.app.alarmnoti.activity.AddCalendarEventListActivity"
            android:label="@string/add_calendar_event"></activity>
        <activity
            android:name="com.wanna.app.alarmnoti.activity.AddAlarmActivity"
            android:label="@string/add_alarm"></activity>

        <receiver android:name="com.wanna.app.alarmnoti.receiver.AlarmReceiver">
            <intent-filter>
                <action android:name="com.wanna.app.alarmnoti.alarm" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
